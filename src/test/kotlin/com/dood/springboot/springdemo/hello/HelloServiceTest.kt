package com.dood.springboot.springdemo.hello

import io.kotest.core.spec.style.FunSpec

/**
 * Goals.
 *  * simple unit test using a mock repository (maybe change the HelloService class to make it 'do more')
 *  * fancier test where I inject a real repo to validate injection (using the @ContextConfiguration vs @SpringBoot
 */
class HelloServiceTest : FunSpec({


})
